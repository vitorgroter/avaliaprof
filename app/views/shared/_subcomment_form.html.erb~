<%= form_for @subcomment do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :comment %>
    <%= f.hidden_field :subject_id %>
  </div>

  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>

